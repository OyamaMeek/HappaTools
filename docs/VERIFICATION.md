# 验证记录

验证日期：2026-09-17。环境：macOS 15.7.7、Xcode 16.4、Swift 6.1.2、Apple Silicon。

## 自动检查

| 检查 | 结果 |
| --- | --- |
| Xcode 工程、Info.plist、entitlements 解析 | 通过 |
| Debug 编译 | 通过 |
| XCTest | 15 项通过，0 项失败 |
| Release universal build | 通过，包含 arm64 和 x86_64 |
| 两个架构的最低系统版本 | Mach-O load command 均为 macOS 12.0 |
| Debug / Release 本地代码签名 | `codesign --verify --deep --strict` 通过 |

测试包括以下真实行为：

- 默认设置及 UserDefaults 重新读取、跨年日期模板。
- 两段 AppleScript 语法编译，以及无窗口、有效目录、特殊位置、缺失目录的返回值处理。
- README 空文件创建、已有内容保护、悬空符号链接保护。
- 命令参数构造，空消息、NUL 字符、选项式分支名的拒绝。
- 名称含空格、引号和符号的真实工作区，带多行及 shell 字面量的提交信息，bare remote 收到同一提交。
- 无变更、非仓库、分支不匹配且不进行暂存。
- 无远端时保留本地提交，补充本地 bare remote 后只重试 push，提交数量不变。
- 在子目录执行时只暂存该子目录。
- 100 KB 输出完整保留，超时中止、可执行文件不存在、stderr 完整保留。

首轮有 1 项非仓库测试失败，因为测试目录中的 Git 会继续向上发现新建的父项目仓库；为测试增加 `GIT_CEILING_DIRECTORIES` 边界后，完整测试再次执行并全部通过。此前工程生成阶段和沙箱缓存限制导致的失败不计为测试通过。

## 本机运行

- Debug 应用已通过 Launch Services 启动，进程持续运行。
- 已读取真实菜单的可访问性树，确认显示当前 Finder 的 `HappaTools` 路径，以及 Commit & Push、Create README.md、Settings、Quit 按钮。
- 后续界面自动化因 `timeoutReached` 中断。采样应用主线程处于正常事件循环等待，没有观察到主线程阻塞。
- 本轮未保存截图或演示视频；不将未操作过的提交弹窗、设置开关、通知横幅等界面路径记为手工测试通过。

## 发布前仍需检查

- 在干净的 macOS 12、13 系统及 Intel 真机运行；当前构建与部署目标检查不能替代实际系统验证。
- 完整演示操作及截图：多窗口 Finder 路径切换、提交输入和取消、进度动画、README 创建后的菜单变化、设置开关、通知展示。
- 系统级拒绝/恢复自动化权限及深链接操作。服务已实现对应错误处理，未重置本机已有权限来伪造首次授权场景。
- 使用分发证书签名及 Apple 公证；本地签名验证不代表已获得 Gatekeeper 分发信任。

构建、测试和采样的中间结果保存在被忽略的 `.build` 目录，不进入版本库。
